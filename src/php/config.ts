import type { GeneratedFile } from '@workos/oagen';

/**
 * PHP webhook utility classes (Webhook.php, WebhookResponse.php) are now
 * hand-maintained in the target SDK with @oagen-ignore-file.
 */
export function generateConfig(): GeneratedFile[] {
  return [];
}
